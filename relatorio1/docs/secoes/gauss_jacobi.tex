\section{Método de Gauss-Jacobi}

O método de Gauss-Jacobi consiste em um procedimento numérico iterativo destinado à resolução de sistemas de equações lineares da forma $Ax = b$. A priori, diferentemente dos métodos diretos que buscam a solução exata em um número finito de passos através de fatorações, os métodos iterativos constroem uma sequência de aproximações sucessivas $\{x^{(k)}\}$ que convergem assintoticamente para o vetor solução ideal, assumindo que determinadas propriedades estruturais da matriz de coeficientes sejam satisfeitas. Essa abordagem torna-se particularmente vantajosa em matrizes esparsas de grande porte, onde a eliminação direta sofreria com o fenômeno de preenchimento (\textit{fill-in}).

\subsection{Fundamentação Teórica}

O desenvolvimento matemático do método baseia-se na decomposição da matriz não singular $A \in \mathbb{R}^{n \times n}$ em três componentes: uma matriz diagonal $D$, uma matriz triangular estritamente inferior $L$ e uma matriz triangular estritamente superior $U$, tal que $A = D - L - U$. Ao substituir essa decomposição no sistema matricial original, obtém-se:

\begin{equation}
    (D - L - U)x = b \implies Dx = b + (L + U)x
\end{equation}

Assumindo que os elementos da diagonal principal são não nulos ($a_{ii} \neq 0$), a matriz $D$ é inversível. Dessa forma, isolando o vetor $x$ do lado esquerdo, formula-se o esquema iterativo de transição de estados:

\begin{equation}
    x^{(k+1)} = D^{-1}b + D^{-1}(L + U)x^{(k)}
\end{equation}

Em termos escalares operacionais, para cada componente $i$, a atualização na iteração $k+1$ é dada por:

\begin{equation}
    x_i^{(k+1)} = \frac{1}{a_{ii}} \left( b_i - \sum_{j \neq i} a_{ij} x_j^{(k)} \right)
\end{equation}

A condição analítica suficiente (embora não estritamente necessária) para garantir a convergência global, independentemente do vetor inicial $x^{(0)}$, é que a matriz $A$ seja estritamente diagonalmente dominante por linhas. O critério das linhas exige que:

\begin{equation}
    |a_{ii}| > \sum_{j \neq i} |a_{ij}|, \quad \forall i \in \{1, 2, \dots, n\}
\end{equation}

A ordem matemática do erro é linear. O fator de redução do erro a cada passo aproxima-se assintoticamente do raio espectral da matriz de iteração $T_J = D^{-1}(L + U)$, denotado por $\rho(T_J)$. Quanto menor for $\rho(T_J) < 1$, mais abrupta será a taxa de convergência.

\subsection{Estratégia de Implementação}

A arquitetura computacional projetada em linguagem Python puro abstém-se de atualizações in-place. O cerne algorítmico demanda que os valores de $x^{(k+1)}$ sejam integralmente computados com base exclusiva nos estados defasados de $x^{(k)}$.

\begin{lstlisting}[language=Python, caption={Algoritmo Resumido do Método de Gauss-Jacobi}, label={lst:gauss_jacobi}]
def gauss_jacobi(A, b, tol=1e-5, max_iter=100, x0=None):
    n = len(b)
    x = x0[:] if x0 else [0.0] * n
    x_new = [0.0] * n
    
    for k in range(max_iter):
        for i in range(n):
            soma = sum(A[i][j] * x[j] for j in range(n) if j != i)
            if abs(A[i][i]) == 0.0:
                raise ValueError(f"Pivo nulo na linha {i}.")
            x_new[i] = (b[i] - soma) / A[i][i]
            
        erro_abs = max(abs(x_new[i] - x[i]) for i in range(n))
        max_x = max(abs(v) for v in x_new)
        erro_rel = erro_abs / max_x if max_x != 0 else erro_abs
        
        if erro_rel < tol or erro_abs < tol:
            return x_new, k + 1
            
        x = x_new[:]
        
    raise ValueError("Nao convergiu dentro do limite de iteracoes.")
\end{lstlisting}

Justifica-se o uso de duas listas distintas (\texttt{x} e \texttt{x\_new}) como uma exigência estrutural da lei de recorrência do método. Critérios de parada duplos foram introduzidos: tolerância pelo erro relativo (com norma infinito) ou limite máximo de iterações (\texttt{max\_iter}). A exceção lógica preemptiva contra elementos $a_{ii}$ nulos previne a detonação da aritmética de ponto flutuante via \textit{ZeroDivisionError}.

\subsection{Estrutura dos Arquivos de Entrada e Saída}

Para resguardar o desacoplamento de responsabilidades, os hiperparâmetros limitadores do critério de parada (como tolerância $\epsilon = 10^{-5}$ e teto iterativo) são lidos sob demanda de um descritor de texto estático (\texttt{.txt}). Como a rotina gera aproximações seriais, o vetor de fase $x_k$ transita sequencialmente e é gravado em linhas iterativas num \textit{dataset} delimitado por vírgulas (\texttt{.csv}). Esse paradigma possibilita uma auditoria póstuma exata de quantas iterações foram requisitadas antes do gatilho de convergência.

\subsection{Problemas Teste}

\subsubsection{Problema Teste 5.1: Discretização da Equação de Laplace}

\textbf{Modelagem e Justificativa de Convergência:} A formulação do problema origina-se da discretização por diferenças finitas de uma Equação Diferencial Parcial (EDP) de Laplace em um domínio bidimensional. A malha mapeia o espaço contínuo para o sistema linear estático de ordem $6 \times 6$ fornecido. A priori, a literatura indaga sobre a garantia de convergência. Observa-se que a matriz dos coeficientes é estritamente diagonalmente dominante por linhas, uma vez que em todas as equações o módulo do elemento principal é estritamente superior à soma dos módulos dos elementos não diagonais. Por exemplo, na primeira linha tem-se $|4| > |-1| + |-1|$, e na quinta linha $|4| > |-1| + |-1| + |-1|$. Pelo Teorema das Linhas, essa propriedade assegura que o raio espectral da matriz de iteração de Jacobi seja estritamente menor que a unidade ($\rho(T_J) < 1$), provando analiticamente a convergência independentemente da semente inicial adotada.

\begin{table}[H]
\centering
\caption{Histórico de Convergência Iterativa (Problema 5.1 - Gauss-Jacobi)}
\label{tab:jacobi_prob51}
\resizebox{\textwidth}{!}{%
\begin{tabular}{cccc}
\hline
\textbf{Iteração ($k$)} & \textbf{Norma da Correção ($\|x^{(k)} - x^{(k-1)}\|_\infty$)} & \textbf{Erro Relativo Global} & \textbf{Aproximação de $x_1$} \\ \hline
1 & 25.0000 & 1.0000 & 25.0000 \\ 
5 & 1.1718 & 0.0318 & 36.8164 \\ 
10 & 0.0939 & 0.0024 & 37.9940 \\ 
15 & 0.0075 & 0.00019 & 38.0871 \\ 
17 & 0.0027 & $7.19 \times 10^{-5}$ & 38.0922 \\ \hline
\end{tabular}%
}
\end{table}

\textbf{Análise Numérica e Validação:} O problema exige uma exatidão pautada em quatro algarismos significativos corretos, o que impõe na engenharia de software um critério de parada baseado no erro relativo tolerado de $\epsilon \leq 10^{-4}$. A avaliação crítica da Tabela \ref{tab:jacobi_prob51} explicita que o método cravou a convergência na 17ª iteração, quando o erro relativo atingiu a marca de $7.19 \times 10^{-5}$. A relação de causa e efeito na propagação da informação matricial é nítida: as condições de contorno (termos forçantes de 100 nas bordas) penetram o domínio discretizado passo a passo. A solução $x_{17} \approx[38.09, 14.28, 4.75, 38.09, 14.28, 4.75]^T$ reflete perfeitamente a simetria da malha térmica/potencial.

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    width=10cm, height=5.5cm,
    ymode=log,
    xlabel={Ciclo Iterativo ($k$)},
    ylabel={Métrica de Erro Relativo $\|e_{rel}\|_\infty$},
    grid=major,
    grid style={dashed, gray!40},
    title={Amortecimento do Resíduo em Malha de Laplace (Jacobi)},
    legend pos=north east
]
\addplot[
    color=teal,
    mark=*,
    thick
] coordinates {
    (1, 1.0)
    (5, 0.0318)
    (10, 0.0024)
    (15, 0.00019)
    (17, 7.19e-5)
};
\addplot[
    color=red,
    domain=1:17,
    dashed
] {1e-4};
\addlegendentry{Erro Observado}
\addlegendentry{Tolerância ($10^{-4}$)}
\end{axis}
\end{tikzpicture}
\caption{Amortecimento do Resíduo em Malha de Laplace (Jacobi)}
\label{fig:jacobi_prob51}
\end{figure}

\subsubsection{Problema Teste 5.2: Balanceamento de Circuito de Kirchhoff}

\textbf{Modelagem e Adequação do Sistema:} A aplicação das Leis de Kirchhoff (LKT) forneceu três equações nodais brutas. A priori, a modelagem direta dos laços não oferece os isolamentos de base. O agrupamento algébrico dos fatores de restrição induz a conversão analítica para o formato $Ax = b$, extraindo a identidade:

\begin{align*}
    20 i_1 - 10 i_2 - 4 i_3 &= 26 \\
    -10 i_1 + 25 i_2 - 5 i_3 &= 0 \\
    -4 i_1 - 5 i_2 + 20 i_3 &= 7
\end{align*}

Torna-se evidente a satisfação do Critério das Linhas: $|20| > |-10| + |-4|$, $|25| > |-10| + |-5|$ e $|20| > |-4| + |-5|$. A estrita dominância diagonal endossa inequivocamente a adoção do Método de Gauss-Jacobi com plena garantia teórica de estabilidade.

\begin{table}[H]
\centering
\caption{Registros Críticos do Método de Gauss-Jacobi (Problema 5.2)}
\label{tab:jacobi_prob52}
\resizebox{\textwidth}{!}{%
\begin{tabular}{ccccc}
\hline
\textbf{Iteração ($k$)} & \textbf{Corrente $i_1$ (A)} & \textbf{Corrente $i_2$ (A)} & \textbf{Corrente $i_3$ (A)} & \textbf{Erro Relativo} \\ \hline
1 & 1.3000 & 0.0000 & 0.3500 & 1.0000 \\ 
3 & 1.7170 & 0.6700 & 0.7715 & 0.2020 \\ 
6 & 1.9275 & 0.9407 & 0.9505 & 0.0274 \\ 
8 & 1.9747 & 0.9783 & 0.9823 & 0.0087 \\ \hline
\end{tabular}%
}
\end{table}

\textbf{Análise Numérica e Validação:} O limite metodológico estabelecido impôs um critério limítrofe de erro relativo $< 10^{-2}$. Os dados empíricos registrados na Tabela \ref{tab:jacobi_prob52} reportam o acionamento do gatilho de convergência precisamente na oitava iteração. A topologia matriz-vetor reflete uma inércia dissipativa rápida: as aproximações estabilizaram-se em $i_k \approx[1.97, 0.97, 0.98]^T$ A. A verificação cruzada com métodos diretos atesta que a raiz teórica é o vetor inteiro $[2, 1, 1]^T$, confirmando o trânsito assintótico regular da matriz condutiva sem sofrer efeitos deletérios de oscilação em vizinhança.

\subsubsection{Problema Teste 5.5: Dissipação Térmica em Membrana Bidimensional}

\textbf{Modelagem e Justificativa de Convergência:} A equação do calor no regime estacionário (Equação de Poisson/Laplace) submetida a uma malha interior de 9 nós derivou uma matriz $9 \times 9$ esparsa. A diagonal principal hospeda estritamente o valor $-4$, correspondente à discretização do operador Laplaciano $(\nabla^2 u)$. Diferentemente dos casos anteriores, a matriz não é estritamente diagonalmente dominante em todas as linhas, uma vez que o nó central (nó 5) possui equação $-4u_5 + u_2 + u_4 + u_6 + u_8 = 0$, resultando na soma não estrita $|-4| = |1|+|1|+|1|+|1|$. No entanto, a convergência iterativa continua rigorosamente assegurada: a matriz é irredutível e fracamente diagonalmente dominante, contendo ao menos uma linha com dominância estrita (os nós de borda).

\begin{table}[H]
\centering
\caption{Decaimento Espacial das Temperaturas (Problema 5.5 - Gauss-Jacobi)}
\label{tab:jacobi_prob55}
\resizebox{\textwidth}{!}{%
\begin{tabular}{ccccc}
\hline
\textbf{Iteração ($k$)} & \textbf{Norma Max ($\|\Delta x\|_\infty$)} & \textbf{Erro Relativo} & \textbf{Nó de Canto ($u_1$)} & \textbf{Nó Central ($u_5$)} \\ \hline
1 & 37.5000 & 1.0000 & 12.5000 & -0.000 \\ 
10 & 0.7827 & 0.0112 & 30.9715 & 47.656 \\ 
20 & 0.0244 & 0.0003 & 32.1062 & 49.926 \\ 
32 & 0.0003 & $5.34 \times 10^{-6}$ & 32.1422 & 49.998 \\ \hline
\end{tabular}%
}
\end{table}

\textbf{Análise Numérica e Sensibilidade:} Observa-se que, devido ao paradigma temporal do Jacobi, que congela o estado contíguo e atualiza todos os nós simultaneamente baseando-se unicamente no instante $k-1$, a propagação da perturbação espelha ondas físicas radiais da borda para o centro. Esse atraso intencional custou à rotina 32 iterações brutas para amortecer a tolerância abaixo de $10^{-5}$. A conformação do vetor iterativo final demonstrou coerência física, cravando as temperaturas estacionárias reais de aproximadamente $-32.14^\circ \text{C}$ nos flancos e decaindo perfeitamente para $-50^\circ \text{C}$ no epicentro $u_5$ e nos quadrantes polares adjacentes.

\subsection{Dificuldades Enfrentadas}

Durante o processo de validação algorítmica, notou-se uma severa limitação no comportamento em que o sistema escapa do escopo de dominância diagonal. Em matrizes de alta correlação cruzada, os elementos fora da diagonal amplificam a perturbação e o método sofreu rápida divergência (a norma $\|\Delta x\|$ tendendo ao infinito iterativamente). 

Adicionalmente, verificou-se que alocar memória constante para duas listas completas de dimensão $N$ (o vetor corrente e o defasado) prejudica a localidade de cache em sistemas de ordem substancial ($N > 10^5$). Para contornar essa falha de ineficiência, bem como acelerar a taxa de atrito em matrizes que beiram as restrições teóricas de convergência, demonstrou-se imperativa a transição do modelo matemático para esquemas que utilizem atualizações assimétricas no estado contínuo de dados.