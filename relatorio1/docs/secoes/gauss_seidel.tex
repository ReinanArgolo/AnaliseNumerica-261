\section{Método de Gauss-Seidel}

O método de Gauss-Seidel consiste em uma evolução algébrica direta do paradigma iterativo estabelecido por Gauss-Jacobi. A priori, a metodologia busca determinar a solução de um sistema $Ax = b$ substituindo as aproximações do vetor simultaneamente durante a própria iteração contínua. Em vez de congelar todo o vetor de estado no instante $k$ para calcular o estado $k+1$, o método de Gauss-Seidel utiliza as variáveis recém-calculadas assim que se tornam disponíveis. Dessa forma, o algoritmo otimiza o fluxo de propagação de informações através da matriz, induzindo, para a grande maioria das topologias bem comportadas, uma taxa de convergência notavelmente superior.

\subsection{Fundamentação Teórica}

O desenvolvimento matemático do método repousa na separação espectral da matriz de coeficientes $A = D - L - U$. No entanto, diferentemente da formulação anterior, a lei de recorrência agrupa a matriz inferior $L$ (onde residem as variáveis já computadas na iteração corrente) com a matriz diagonal $D$. A formulação analítica do sistema iterativo é deduzida da relação algébrica:

\begin{equation}
    (D - L)x^{(k+1)} = b + U x^{(k)} \implies x^{(k+1)} = (D - L)^{-1} b + (D - L)^{-1} U x^{(k)}
\end{equation}

Em termos práticos de somatória elementar para cada linha $i$, torna-se evidente que a parcela dependente das variáveis $x_1, \dots, x_{i-1}$ referencia o estado evoluído $k+1$, enquanto a parcela $x_{i+1}, \dots, x_n$ referencia o estado defasado $k$:

\begin{equation}
    x_i^{(k+1)} = \frac{1}{a_{ii}} \left( b_i - \sum_{j=1}^{i-1} a_{ij} x_j^{(k+1)} - \sum_{j=i+1}^{n} a_{ij} x_j^{(k)} \right)
\end{equation}

As condições de convergência são ditadas pelo raio espectral da matriz de transição $T_{GS} = (D - L)^{-1} U$. A convergência global assintótica é rigorosamente garantida se a matriz $A$ for estritamente diagonalmente dominante, simétrica positiva definida, ou se obedecer ao Critério de Sassenfeld, onde os coeficientes de ponderação $\beta_i$ devem atestar um máximo $\max(\beta_i) < 1$. Por mais que a ordem do erro seja de caráter empírico linear, na norma geral, a constante subjacente à atenuação do erro torna-se inferior à obtida pelo método de Jacobi.

\subsection{Estratégia de Implementação}

A arquitetura do método elimina a dualidade de alocação temporal. A reatribuição \textit{in-place} é natural em Python, uma vez que a sobrescrita das variáveis na mesma lista desencadeia o efeito dominó imposto pela fórmula analítica do método.

\begin{lstlisting}[language=Python, caption={Algoritmo Resumido In-Place de Gauss-Seidel}, label={lst:gauss_seidel}]
def gauss_seidel(A, b, tol=1e-5, max_iter=100, x0=None):
    n = len(A)
    x = list(x0) if x0 else[0.0 for _ in range(n)]
    
    for iteracao in range(1, max_iter + 1):
        x_ant = list(x)
        
        for i in range(n):
            soma = sum(A[i][j] * x[j] for j in range(n) if j != i)
            if abs(A[i][i]) == 0.0:
                raise ValueError(f"Pivot nulo encontrado na linha {i}.")
            x[i] = (b[i] - soma) / A[i][i]
            
        max_diff = max(abs(x[i] - x_ant[i]) for i in range(n))
        max_x = max(abs(v) for v in x)
        erro = max_diff / max_x if max_x != 0 else max_diff
        
        if erro < tol:
            return x, iteracao
            
\end{lstlisting}

Justifica-se a abordagem contígua (\texttt{soma = sum(A[i][j] * x[j] ...)}) pois o referenciamento ao iterável \texttt{x[j]} capta os índices $j < i$ naturalmente já recalculados no laço corrente. A persistência isolada de \texttt{x\_ant} serve unicamente à determinação da norma infinito para o critério de tolerância $\epsilon$. O encerramento condicional ocorre sem comprometer a estrutura temporal da matriz original.

\subsection{Estrutura dos Arquivos de Entrada e Saída}

Seguindo o paradigma modular consolidado, a malha de hiperparâmetros (vetor inicial, limites) reside em arquivos vetoriais. Notavelmente, a gravação de saídas deste método condensa vetores sucessivos numa planilha (\texttt{.csv}). Os registros mostram variações residuais acentuadas logo nas fases embrionárias, demonstrando um refinamento superior na primeira metade do ciclo temporal, cuja interpretação é delegada ao documento base por via dos scripts de diagramação subjacentes.  
\subsection{Problemas Teste}

\subsubsection{Problema Teste 5.1: Discretização da Equação de Laplace}

\textbf{Modelagem e Contraponto Estrutural:} Mantendo a premissa fundamental de discretização adotada no método anterior, o sistema retangular da Equação de Laplace configura um campo de prova ideal para atestar a agressividade analítica do método de Gauss-Seidel. Enquanto Jacobi aguarda o término do laço de varredura nodal para atualizar os dados em memória, Gauss-Seidel injeta a aproximação calculada da variável $x_i^{(k)}$ imediatamente na determinação algébrica de $x_{i+1}^{(k)}$. Essa quebra de simetria cronológica promove, na esmagadora maioria das matrizes dominantes, um atrito de erro consideravelmente mais vertiginoso.

\begin{table}[H]
\centering
\caption{Histórico de Convergência Acelerada (Problema 5.1 - Gauss-Seidel)}
\label{tab:seidel_prob51}
\resizebox{\textwidth}{!}{%
\begin{tabular}{cccc}
\hline
\textbf{Iteração ($k$)} & \textbf{Erro Relativo Global} & \textbf{Custo Reduzido} & \textbf{Aproximação de $x_1$} \\ \hline
1 & 1.0000 & - & 25.0000 \\ 
3 & 0.0654 & - & 36.8164 \\ 
5 & 0.0077 & - & 37.9276 \\ 
8 & 0.0003 & - & 38.0871 \\ 
10 & $4.92 \times 10^{-5}$ & Fator 1.7x mais rápido & 38.0941 \\ \hline
\end{tabular}%
}
\end{table}

\textbf{Análise de Desempenho Escalar:} Uma avaliação crítica frente aos resultados pregressos ilustra o porquê de Gauss-Seidel suplantar métodos estacionários homogêneos. O critério imperativo de 4 algarismos significativos corretos ($\epsilon < 10^{-4}$) foi abatido na 10ª iteração, contra 17 ciclos exigidos por Jacobi. Essa compressão do tempo computacional provém diretamente do coeficiente de atrito do raio espectral induzido $\rho(T_{GS}) \approx (\rho(T_J))^2$. O fluxo unidirecional transferiu a perturbação de contorno (termos independentes) pelos índices lineares mais rapidamente, estabilizando as raízes da EDP num espectro vetorial indissociável de exatidão.

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    width=10cm, height=5.5cm,
    ymode=log,
    xlabel={Iterações Processadas ($k$)},
    ylabel={Escala de Erro Relativo $\|e_{rel}\|$},
    grid=both,
    grid style={dashed, gray!30},
    title={Comparativo de Taxa de Convergência Iterativa},
    legend pos=north east
]
\addplot[
    color=orange,
    mark=square,
    thick
] coordinates {
    (1, 1.0)
    (2, 0.2608)
    (5, 0.0077)
    (8, 0.0003)
    (10, 4.92e-5)
};
\addplot[
    color=blue,
    mark=*,
    dashed
] coordinates {
    (1, 1.0)
    (5, 0.0318)
    (10, 0.0024)
    (15, 0.00019)
    (17, 7.19e-5)
};
\addlegendentry{Gauss-Seidel (Imediato)}
\addlegendentry{Gauss-Jacobi (Atrasado)}
\end{axis}
\end{tikzpicture}
\caption{Comparativo de Taxa de Convergência Iterativa}
\label{fig:seidel_comparativo}
\end{figure}

\subsubsection{Problema Teste 5.2: Balanceamento de Circuito de Kirchhoff}

\textbf{Modelagem e Critério de Sassenfeld:} Submetendo a topologia linear de correntes (equacionada por $20i_1-10i_2-4i_3 = 26$) ao escrutínio analítico, a indicação formal de convergência ganha estofo matemático superior. O método de Gauss-Seidel prescinde estritamente do critério das linhas isolado e alavanca o Critério de Sassenfeld. Ao apurar as grandezas ponderadas iterativas $\beta_i$, nota-se que a substituição progressiva decai substancialmente o limiar local de convergência, cimentando teoricamente o alcance restritivo do algoritmo. A simetria natural $A = A^T$ somada aos determinantes positivos também comprova dominância de positividade definida, atestando convergência universal incondicional.

\begin{table}[H]
\centering
\caption{Registros Críticos do Método de Gauss-Seidel (Problema 5.2)}
\label{tab:seidel_prob52}
\resizebox{\textwidth}{!}{%
\begin{tabular}{ccccc}
\hline
\textbf{Iteração ($k$)} & \textbf{Corrente $i_1$ (A)} & \textbf{Corrente $i_2$ (A)} & \textbf{Corrente $i_3$ (A)} & \textbf{Erro Relativo} \\ \hline
1 & 1.3000 & 0.5200 & 0.7400 & 1.0000 \\ 
3 & 1.8954 & 0.9380 & 0.9636 & 0.0989 \\ 
5 & 1.9860 & 0.9917 & 0.9951 & 0.0122 \\ 
6 & 1.9949 & 0.9969 & 0.9982 & 0.0044 \\ \hline
\end{tabular}%
}
\end{table}

\textbf{Análise Numérica e Validação:} Demandada sob uma barreira qualitativa rigorosa (erro $< 10^{-2}$), a convergência por atualizações defasadas cumpriu a injunção em exatas 6 iterações, eclipsando a oitava passagem histórica de seu homólogo clássico Jacobi. Observa-se que, ao determinar iterativamente $i_2 = (10 \cdot i_1^{(k)} + 5 \cdot i_3^{(k-1)})/25$, a injeção instantânea do valor já depurado de $i_1$ estancou as ondulações residuais da malha elétrica precocemente. A aproximação fixou seu trânsito em torno de $I^* = [2, 1, 1]^T$, solidificando a confiabilidade e robustez do algoritmo assíncrono.

\subsubsection{Problema Teste 5.5: Dissipação Térmica em Membrana Bidimensional}

\textbf{Modelagem e Fluxo de Propagação Numérica:} A complexidade inerente da equação elíptica que comanda a difusão da carga térmica nas dimensões ortogonais $x$ e $y$ mapeia uma matriz irredutível de grau 9. Torna-se evidente no seio do processamento numérico de EDPs (Equações Diferenciais Parciais) que as atualizações instantâneas no núcleo de laços espaciais mimetizam o transporte físico do calor mais fidedignamente. Assim, um gradiente calculado no canto superior da membrana interage matematicamente de imediato com a célula vizinha processada no segundo milissegundo de execução.

\begin{table}[H]
\centering
\caption{Convergência Espacial Acelerada do Campo (Problema 5.5 - Gauss-Seidel)}
\label{tab:seidel_prob55}
\resizebox{\textwidth}{!}{%
\begin{tabular}{ccccc}
\hline
\textbf{Iteração ($k$)} & \textbf{Atenuação ($\|\Delta x\|_\infty$)} & \textbf{Erro Relativo} & \textbf{Aproximação Contorno ($u_1$)} & \textbf{Centro ($u_5$)} \\ \hline
1 & 41.4062 & 1.0000 & 12.5000 & 4.6875 \\ 
5 & 2.1931 & 0.0490 & 28.7765 & 46.582 \\ 
10 & 0.0715 & 0.0014 & 32.0360 & 49.893 \\ 
18 & 0.0002 & $5.84 \times 10^{-6}$ & 32.1424 & 49.999 \\ \hline
\end{tabular}%
}
\end{table}

\textbf{Análise Numérica e Sensibilidade Vetorial:} A síntese cronológica ditada pela Tabela \ref{tab:seidel_prob55} documenta um corte massivo no esforço bruto da unidade de processamento flutuante. Dos 32 estágios contínuos exigidos outrora por Jacobi, o engajamento contíguo de Seidel comprimiu a saturação térmica em 18 ciclos. Observa-se ainda que o decaimento não exibe ressonâncias paramétricas, fluindo mono-direcionalmente em obediência às condições de valor de contorno isoladas em $u = -150^\circ\text{C}$ nas extremidades. A matriz de resposta demonstrou coerência física idêntica aos processos exatos de Fatoração LU ($u_5 \approx -50^\circ\text{C}$), coroando o método como a aproximação canônica ótima frente a topologias de banda estreitamente esparsas na ausência de hiper-relaxação sucessiva (SOR).

\subsection{Dificuldades Enfrentadas}

A síntese crítica de desempenho identificou que as vantagens inerentes a Seidel implicam perdas agudas na escalabilidade contemporânea. Diferentemente do Jacobi, o método não pode ser decomposto linearmente para processos massivamente paralelos (GPGPU), já que a iteração em $x_i$ depende imperativamente que a \textit{thread} responsável por $x_{i-1}$ termine sua execução. Dessa forma, observa-se ainda que em estruturas mal condicionadas limítrofes, a injeção imediata de um salto espúrio em $x_1$ induz um efeito chicote desestabilizando todos os valores decorrentes da mesma iteração, ampliando transitoriamente as flutuações antes que a restrição de Sassenfeld possa reordenar o trajeto contínuo.